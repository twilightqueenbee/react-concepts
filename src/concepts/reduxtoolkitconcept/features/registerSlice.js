import { createSlice } from "@reduxjs/toolkit";

const registerSlice=createSlice({
    name:"register",
    initialState,
    reducers:{
        handleinput:(state,action)=>{
            
        }
    }
})